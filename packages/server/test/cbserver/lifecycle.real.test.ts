/**
 * Real cbserver — lifecycle: ENROLL, CANCEL_ME graceful close, supervisor stop.
 */
/// <reference types="mocha" />
import { expect } from "chai";
import { describeReal, gracefulCloseConnection, installRealTestGuards, MS, openConnection, PER_TEST_TIMEOUT_MS, raceTimeout, runCommand, waitForRegistrySize, waitForServerState, withRealServer, withRealSession, } from "./realHarness";

describeReal( "CBServer lifecycle [real cbserver]", function () { this.timeout(PER_TEST_TIMEOUT_MS); installRealTestGuards(this); it("ENROLL on connect, CANCEL_ME on close, supervisor reaches Stopped", async function () { await withRealSession("lifecycle-cancel", async ({ server, connection }) => { await runCommand(server, connection, "pwd-after-enroll", () => connection.pwd()); }); }); it("close is idempotent-safe on supervisor registry (one connection slot)", async function () { await withRealServer("lifecycle-registry", async (server) => { const connection = await openConnection(server); expect(server.ctx.connections.size).to.equal(1); await gracefulCloseConnection(server, connection); expect(server.ctx.connections.size).to.equal(0); }); }); it("two connections: close each with CANCEL_ME before server stop", async function () { await withRealServer("lifecycle-dual", async (server) => { const left = await openConnection(server); const right = await openConnection(server); expect(server.ctx.connections.size).to.equal(2); await runCommand(server, left, "left-pwd", () => left.pwd()); await runCommand(server, right, "right-pwd", () => right.pwd()); await gracefulCloseConnection(server, left); expect(server.ctx.connections.size).to.equal(1); await gracefulCloseConnection(server, right); expect(server.ctx.connections.size).to.equal(0); }); }); it("stop waits for subprocess exit (no forced SIGKILL on happy path)", async function () { let pid: number | undefined; await withRealServer("lifecycle-stop", async (server) => { pid = server.ctx.pid; expect(pid).to.be.a("number"); const connection = await openConnection(server); await gracefulCloseConnection(server, connection); }); await raceTimeout(new Promise<void>((resolve, reject) => { try { process.kill(pid!, 0); reject(new Error(`cbserver pid ${pid} still running after graceful stop`)); } catch { resolve(); } }), "process-reaped", MS.stop,); }); it("server stop closes open connections before SIGTERM", async function () { await withRealServer("lifecycle-stop-open-conn", async (server) => { expect(server.ctx.pid).to.be.a("number"); let connectionsAtKill = -1; const originalKill = server.port.kill.bind(server.port); server.port.kill = async (targetPid, signal) => { connectionsAtKill = server.ctx.connections.size; return originalKill(targetPid, signal); }; const connection = await openConnection(server); await runCommand(server, connection, "pwd-before-stop", () => connection.pwd()); expect(server.ctx.connections.size).to.equal(1); server.actor.notify.stop(); await waitForRegistrySize(server, "stop-closes-connections", 0); await waitForServerState(server, "Stopped", "stop-with-open-conn"); expect(connectionsAtKill).to.equal(0); }); }); } );
