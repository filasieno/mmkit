/**
 * CBServerConnection — dual-channel orchestrator with mock TCP sockets.
 */
/// <reference types="mocha" />
import { expect } from "chai";
import * as ihsm from "ihsm/testing";
import { CBConnectionContext, waitForCommandAnswer, waitForConnectionBootstrap, waitForConnectionClose, waitForNotificationAnswer, } from "../../src/cbserver/actors/connection/CBServerConnectionContext";
import { ConnectionIdle } from "../../src/cbserver/actors/connection/CBServerConnectionActor";
import { CBConnectionTop } from "../../src/cbserver/actors/connection/CBServerConnectionConfig";
import { makeMockConnectionOrchestratorPort } from "./mockConnectionOrchestratorPort";
import { CB_IPC_METHODS } from "../../src/cbserver/shared/cbIpcCatalog";
import { formatTcpLengthFrame } from "../../src/cbserver/actors/reader/tcpFraming";
import { encodeCbString } from "@mmkit/base";
import { assertMockCommandChannelCoverage, MOCK_COMMAND_CHANNEL_DISPATCH_CASES } from "./mockCommandCatalog";
import { makeMockCommandChannelPort, makeMockNotificationChannelPort } from "./mockTcpSocket";

describe( "CBServerConnection [mock TCP socket]", function () { this.timeout(5_000); function makeConnectionContext(connectionId: string, onClose: () => void): CBConnectionContext { return new CBConnectionContext(connectionId, onClose, { host: "127.0.0.1", port: 4001 }, { command: makeMockCommandChannelPort("mock-cmd-client"), notification: makeMockNotificationChannelPort("mock-notify-client"), },); } it("initialize enrolls both channels and reaches ConnectionIdle", async () => { const closed: string[] = []; const ctx = makeConnectionContext("conn-1", () => closed.push("closed")); const actor = ihsm.makeTestActor(CBConnectionTop, ctx, makeMockConnectionOrchestratorPort()); await actor.hsm.sync(); const bootstrap = waitForConnectionBootstrap(ctx); await actor.call.initialize(); await bootstrap; await actor.hsm.sync(); expect(actor.hsm.currentStateName).to.equal("ConnectionIdle"); expect(ctx.commandCtx?.enrolled).to.equal(true); expect(ctx.notificationCtx?.enrolled).to.equal(true); }); it("pwd sends GET_MODULE_PATH via command channel", async () => { const ctx = makeConnectionContext("conn-2", () => undefined); const actor = ihsm.makeTestActor(CBConnectionTop, ctx, makeMockConnectionOrchestratorPort()); await actor.hsm.sync(); const bootstrap = waitForConnectionBootstrap(ctx); await actor.call.initialize(); await bootstrap; await actor.hsm.sync(); const answerP = waitForCommandAnswer(ctx); actor.notify.dispatchPwd(); const answer = await answerP; await actor.hsm.sync(); expect(answer.ok).to.equal(true); const commandPort = ctx.channelPorts!.command! as ReturnType<typeof makeMockCommandChannelPort>; expect(commandPort.write.calls.length).to.be.greaterThan(1); }); it("close sends CANCEL_ME on both channels and reaches ConnectionClosed", async () => { const closed: string[] = []; const ctx = makeConnectionContext("conn-3", () => closed.push("closed")); const destroyOrder: string[] = []; const cmdPort = ctx.channelPorts!.command! as ReturnType<typeof makeMockCommandChannelPort>; const notifPort = ctx.channelPorts!.notification! as ReturnType<typeof makeMockNotificationChannelPort>; cmdPort.destroy.default(() => { destroyOrder.push("command"); }); notifPort.destroy.default(() => { destroyOrder.push("notification"); }); const actor = ihsm.makeTestActor(CBConnectionTop, ctx, makeMockConnectionOrchestratorPort()); await actor.hsm.sync(); const bootstrap = waitForConnectionBootstrap(ctx); await actor.call.initialize(); await bootstrap; await actor.hsm.sync(); const closeP = waitForConnectionClose(ctx); actor.notify.close(); await closeP; await actor.hsm.sync(); expect(actor.hsm.currentStateName).to.equal("ConnectionClosed"); expect(cmdPort.destroy.calls.length).to.equal(1); expect(notifPort.destroy.calls.length).to.equal(1); expect(destroyOrder).to.deep.equal(["command", "notification"]); expect(closed).to.deep.equal(["closed"]); }); it("command channel socket error breaks connection with ConnectionBroken", async () => { const ctx = makeConnectionContext("conn-4", () => undefined); const actor = ihsm.makeTestActor(CBConnectionTop, ctx, makeMockConnectionOrchestratorPort()); await actor.hsm.sync(); const bootstrap = waitForConnectionBootstrap(ctx); await actor.call.initialize(); await bootstrap; await actor.hsm.sync(); const cmdPort = ctx.channelPorts!.command! as ReturnType<typeof makeMockCommandChannelPort>; cmdPort.send("onSocketError", "ECONNRESET"); await actor.hsm.sync(); await actor.hsm.sync(); expect(actor.hsm.currentStateName).to.equal("ConnectionBroken"); expect(ctx.brokenReason).to.match(/ECONNRESET/); }); it("reportClients uses REPORT_CLIENTS ipc method on command channel", async () => { const ctx = makeConnectionContext("conn-5", () => undefined); const actor = ihsm.makeTestActor(CBConnectionTop, ctx, makeMockConnectionOrchestratorPort()); await actor.hsm.sync(); const bootstrap = waitForConnectionBootstrap(ctx); await actor.call.initialize(); await bootstrap; await actor.hsm.sync(); const answerP = waitForCommandAnswer(ctx); actor.notify.dispatchReportClients(); const answer = await answerP; await actor.hsm.sync(); expect(answer.ok).to.equal(true); const cmdPort = ctx.channelPorts!.command! as ReturnType<typeof makeMockCommandChannelPort>; const writes = cmdPort.write.calls.map((c: [Buffer]) => c[0].toString("utf8")); expect(writes.some((w: string) => w.includes(CB_IPC_METHODS.REPORT_CLIENTS))).to.equal(true); }); it("dispatches every command IPC via the command channel only", async () => { const ctx = makeConnectionContext("conn-6", () => undefined); const actor = ihsm.makeTestActor(CBConnectionTop, ctx, makeMockConnectionOrchestratorPort()); await actor.hsm.sync(); const bootstrap = waitForConnectionBootstrap(ctx); await actor.call.initialize(); await bootstrap; await actor.hsm.sync(); const cmdPort = ctx.channelPorts!.command! as ReturnType<typeof makeMockCommandChannelPort>; const notifPort = ctx.channelPorts!.notification! as ReturnType<typeof makeMockNotificationChannelPort>; const ipcCovered = new Set<string>(); for (const testCase of MOCK_COMMAND_CHANNEL_DISPATCH_CASES) { const cmdWritesBefore = cmdPort.write.calls.length; const notifWritesBefore = notifPort.write.calls.length; const answerP = waitForCommandAnswer(ctx); testCase.run(actor); const answer = await answerP; await actor.hsm.sync(); expect(answer.ok, `${testCase.label} completion=${answer.completion}`).to.equal(true); expect(cmdPort.write.calls.length, `${testCase.label} must write command channel`).to.be.greaterThan(cmdWritesBefore); expect(notifPort.write.calls.length, `${testCase.label} must not write notification channel`).to.equal(notifWritesBefore); ipcCovered.add(testCase.ipc); } assertMockCommandChannelCoverage(ipcCovered); const stopAnswerP = waitForCommandAnswer(ctx); actor.notify.dispatchStopServer(""); const stopAnswer = await stopAnswerP; for (let i = 0; i < 10 && actor.hsm.currentStateName !== "ConnectionClosed"; i += 1) { await actor.hsm.sync(); } expect(stopAnswer.ok).to.equal(true); expect(actor.hsm.currentStateName).to.equal("ConnectionClosed"); }); it("getNotificationMessage reads from the notification channel only", async () => { const ctx = makeConnectionContext("conn-6b", () => undefined); const actor = ihsm.makeTestActor(CBConnectionTop, ctx, makeMockConnectionOrchestratorPort()); await actor.hsm.sync(); const bootstrap = waitForConnectionBootstrap(ctx); await actor.call.initialize(); await bootstrap; await actor.hsm.sync(); const cmdPort = ctx.channelPorts!.command! as ReturnType<typeof makeMockCommandChannelPort>; const notifPort = ctx.channelPorts!.notification! as ReturnType<typeof makeMockNotificationChannelPort>; const cmdWritesBefore = cmdPort.write.calls.length; const answerP = waitForNotificationAnswer(ctx); actor.notify.dispatchGetNotificationMessage(500); await new Promise<void>((resolve) => { setImmediate(resolve); }); const payload = `ipcanswer("cbserver",notification,${encodeCbString("view(EmpView)")}).`; notifPort.send("onSocketData", formatTcpLengthFrame(payload)); const answer = await answerP; await actor.hsm.sync(); expect(answer.completion).to.equal("notification"); expect(answer.result).to.equal("view(EmpView)"); expect(cmdPort.write.calls.length).to.equal(cmdWritesBefore); }); it("getClientId and getNotificationClientId return distinct enroll ids", async () => { const ctx = makeConnectionContext("conn-7", () => undefined); const actor = ihsm.makeTestActor(CBConnectionTop, ctx, makeMockConnectionOrchestratorPort()); await actor.hsm.sync(); const bootstrap = waitForConnectionBootstrap(ctx); await actor.call.initialize(); await bootstrap; await actor.hsm.sync(); const cmdId = await actor.call.getClientId(); const notifId = await actor.call.getNotificationClientId(); const connId = await actor.call.getConnectionId(); expect(connId).to.equal("conn-7"); expect(cmdId).to.equal("mock-cmd-client"); expect(notifId).to.equal("mock-notify-client"); expect(notifId).to.not.equal(cmdId); }); } );
