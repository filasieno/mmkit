# Archived mmkit packages

Previous implementation moved here during the 0.2.0 rewrite (June 2026).

- `packages/extension` — full VS Code extension (actors, panel, LSP client, notebooks)
- `packages/lsp-server` — `@mmkit/lsp-server` (MCP, HTTP, LSP, cbserver supervisor)
- `packages/shared` — shared protocol and constants

New code lives in `../packages/` (`shared`, `server`, `extension`).
